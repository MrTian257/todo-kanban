# To Do 描述 Markdown 测试计划

日期：2026-09-09  
状态：**本次单栏即时编辑改动未运行构建或测试。第 9 节仅为旧版编辑器的历史结果，不代表当前版本通过。**

## 当前改动：单栏即时编辑（待验证）

任务描述已改用 Vditor 的即时渲染（IR）模式，取消源文/分栏/预览切换。输入 Markdown 时在同一正文区域呈现格式，光标附近可继续编辑语法。看板的只读描述继续使用原渲染器。

- 保留 Markdown 存储、工具栏、撤销/重做、图片选择/粘贴/拖入以及任务草稿。
- 编辑器初始化、中文组合输入和图片处理期间阻止保存；加载超时提供重试。
- 运行资源通过 `scripts/sync-vditor-assets.mjs` 在 `npm run dev` / `npm run build` 前复制到本地。不要直接执行 Vite 绕过该步骤。
- 未编辑时不因编辑器初始化而改写原描述。实际编辑后会重新序列化 Markdown，空行、列表标记、引用写法等可能被规范化，不保证源码逐字一致。
- IR 编辑器与看板只读渲染器不同，扩展语法兼容性仍须分别验收，不应将“完整 Markdown”理解为支持所有私有方言。

待执行清单（本轮全部未执行）：

1. 安装依赖、运行前端构建；浏览器与 Tauri 分别确认本地解析器、中文资源、公式和字体加载成功。
2. 输入标题、粗体、斜体、删除线、嵌套列表、待办复选框、引用、分隔线、链接和表格：格式在正文呈现，不出现分栏。
3. 输入中文拼音并选词、连续撤销/重做、粘贴多段 Markdown、修改代码块和公式；光标与内容不得丢失。
4. 插入脚注、引用链接、安全 HTML、已有 base64 图片；保存重开并对照看板显示，记录源码规范化与不兼容语法。
5. 输入后立即点击保存、切换焦点、保存草稿及恢复草稿；最后一次输入应完整保留，工具栏按钮不得提交任务。
6. 选择、粘贴、拖入图片；核对多图、透明 PNG、GIF/WebP、大小限制、失败提示及处理期间保存禁用。
7. 图片处理中切换任务、恢复草稿或更新描述：不得把图片插入旧任务或覆盖新内容。
8. 深浅主题、窄窗口、长代码、宽表格、长链接及键盘操作：工具栏可用，无页面横向溢出。
9. 编辑器资源加载失败/超时后重试、快速切换任务、React StrictMode 重挂载：原描述保留，无重复编辑器或保存锁残留。
10. 点击描述内链接或脚注不应离开任务；模式快捷键不得切换到源码分栏。

以下章节保留旧版源码编辑器的历史计划与结果，其中模式切换和源码逐字保留等断言不再作为新版验收标准。

## 1. 改动与验收范围

描述编辑从 contenteditable 富文本往返转换，改为受控 Markdown 源文输入。新增编辑、分栏、预览三种模式；预览与看板只读描述共用渲染器。表单使用当前 note 值，直接保存源文。

涉及文件：

- `src/components/todo/MarkdownEditor.tsx`：源文输入、工具栏、快捷键、图片插入、模式切换。
- `src/components/todo/MarkdownView.tsx`：CommonMark、GFM、公式、代码高亮、安全 HTML 与内嵌图片。
- `src/pages/TodoDetailPage.tsx`：描述与表单状态绑定。
- `src/index.css`：预览排版、表格滚动、主题和窄屏分栏。
- `package.json`、`package-lock.json`：新增渲染依赖。

支持目标为 CommonMark + GFM（表格、删除线、任务列表、自动链接、脚注），另加 KaTeX 数学公式与经清洗的 HTML。这里的“完整”不指所有私有 Markdown 方言：Mermaid 图形、MDX/JSX、任意 HTML/CSS/脚本、自定义容器等未实现；其源文应保留，不能宣称具备相应渲染能力。

## 2. 测试准备

1. 使用测试项目与测试待办，桌面端使用测试数据库，记录运行目录及数据备份位置。
2. 准备一条新待办、一条已有复杂 Markdown 的待办、一条含历史 base64 图片的待办。
3. 为保存流程配置有效的代码目录、分支与泳道，避免必填字段错误干扰描述测试。
4. 浏览器覆盖 Chromium；桌面覆盖 macOS Tauri WebView。重点复核原生文本插入和撤销差异。
5. 窗口覆盖 1440×900、1024×768、390×844；主题覆盖明亮、深色。
6. 准备小 PNG、透明 PNG、大尺寸 JPEG、GIF/WebP、损坏图片及非图片文件。

以下命令仅供后续执行，本次没有运行：

```sh
cd '/Users/unstable/Documents/ChatGPT/TO DO/todo-kanban'
npm ci
npm run build
npm run dev
# 在另外的终端按项目要求启动桌面环境：
npm run tauri dev
```

以 npm 和 `package-lock.json` 作为本次交付的依赖安装入口；若使用 Bun，需要确认 `bun.lock` 与当前依赖一致。

## 3. P0：数据与核心交互

所有用例初始状态均为“未执行”。

| 编号 | 操作 | 预期结果 |
| --- | --- | --- |
| P0-01 | 新建待办，粘贴第 6 节样例，保存并重新打开 | 描述源文逐字符一致，空行、缩进、转义、代码围栏、引用定义不丢失 |
| P0-02 | 打开已有复杂描述，仅修改标题后保存 | 描述不发生隐式转换或格式丢失 |
| P0-03 | 修改描述，反复切换编辑→分栏→预览→编辑 | 输入不丢失；模式切换不自行修改 note；预览与最新输入一致 |
| P0-04 | 描述清空后保存，再打开 | 正确保存空字符串；预览显示空备注提示，不恢复旧内容 |
| P0-05 | 在两条待办之间切换，分别修改并保存 | 无串值；各待办显示并保存自己的描述 |
| P0-06 | 修改描述后尝试返回、刷新或关闭窗口 | 按现有未保存保护流程提示；取消离开保留输入 |
| P0-07 | 保存后在看板打开只读描述 | 表格、公式、代码、列表、图片与编辑器预览保持一致 |
| P0-08 | 桌面保存复杂描述，退出并重启应用 | SQLite 中的 note 与重新显示的源文一致；浏览器 sessionStorage 测试不能替代此项 |
| P0-09 | 保存失败后返回编辑区 | 错误可见，用户仍可看到并复制未保存源文 |
| P0-10 | 输入中文并使用输入法候选、回车确认，连续编辑长文 | 不吞字、不重复、不跳光标；组合输入不被快捷键打断 |

## 4. P1：语法与渲染

| 编号 | 操作/输入 | 预期结果 |
| --- | --- | --- |
| MD-01 | ATX 一至六级标题、Setext 标题、段落、空行 | 层级与段落正确，标题有可辨识字号 |
| MD-02 | 强调、加粗、嵌套强调、删除线、反斜杠转义、HTML 实体 | 按 CommonMark/GFM 规则渲染；转义字符不误触发格式 |
| MD-03 | 两个尾随空格或反斜杠硬换行、普通单换行 | 硬换行生效；普通单换行按 CommonMark 软换行处理 |
| MD-04 | 无序、有序、非 1 起始序号、嵌套列表、多段列表项 | 列表结构、起始序号、层级和段落正确 |
| MD-05 | 已完成和未完成任务列表、嵌套任务 | 显示正确复选状态；预览复选框为只读，源文可修改状态 |
| MD-06 | 多层引用、引用中列表、分隔线 | 层级清晰，分隔线不误解析为标题或列表 |
| MD-07 | 行内代码、反引号嵌套、缩进代码、围栏代码 | 特殊字符不丢失；空格缩进保留；长行在代码块内滚动 |
| MD-08 | javascript/json/bash 代码及未知语言围栏 | 已支持语言有高亮；未知语言仍显示代码，无运行时错误 |
| MD-09 | 表格左/中/右对齐、单元格内转义竖线、长表格 | 列与对齐正确；窄屏只在表格容器横向滚动 |
| MD-10 | 内联链接、引用链接、自动 URL、邮箱、图片标题 | 地址与文字正确；外部链接不替换当前编辑页面 |
| MD-11 | 脚注及回链，同一页面多个 Markdown 实例 | 跳转到本实例对应位置；URL 路由不变，不跳回首页 |
| MD-12 | 行内公式、块级公式、复杂分式、错误公式 | 合法公式排版正确；错误公式不使整个描述崩溃 |
| MD-13 | details/summary、kbd、sub/sup、br 等安全 HTML | 支持的标签正确显示；折叠内容可展开 |
| MD-14 | HTML 中 script、事件处理器、iframe、javascript: 链接及 SVG data URL | 不执行脚本、不触发导航或外部嵌入；正文仍可阅读 |
| MD-15 | PNG/JPEG/GIF/WebP base64 图片与普通远程图片 | 允许的图片源显示；历史内嵌图片不因 URL 过滤消失 |

## 5. P1/P2：编辑工具、图片和展示

| 编号 | 操作 | 预期结果 |
| --- | --- | --- |
| ED-01 | 选中一段文字，点击加粗、斜体、删除线、行内代码和链接 | 在原选区插入正确标记，选中文字正确，不跳到文末 |
| ED-02 | 无选区时使用工具栏，再替换占位文字 | 光标/选区落在可编辑占位内容上 |
| ED-03 | 从文首、行中、文尾选中单行或多行，应用标题、列表、引用 | 前缀作用于对应整行；多行有序列表递增；不改动邻接行 |
| ED-04 | 在段落中间插入表格、分隔线、公式和代码块 | 自动分隔为独立块；原前后文本完整保留 |
| ED-05 | 选中含三反引号的文本后点击代码块 | 外层围栏足够长，内部围栏不提前结束代码块 |
| ED-06 | 使用 ⌘/Ctrl+B、I、K；工具栏后撤销/重做 | 快捷键与按钮一致；撤销/重做后源文、表单状态和预览一致 |
| ED-07 | 仅用键盘访问模式、工具栏、帮助和编辑区 | 焦点可见，按钮有可读名称；不误提交表单 |
| ED-08 | 预览模式下查看界面 | 编辑工具隐藏；源文未变；回到编辑后可继续输入 |
| IMG-01 | 在文中光标处粘贴图片、通过按钮选择图片 | 图片 Markdown 插在所选位置；预览可见；保存重开仍可见 |
| IMG-02 | 一次插入多张图片，文件名含方括号、反斜杠等 | 顺序稳定，语法有效；已有文本不丢失 |
| IMG-03 | 插入大图和透明 PNG | PNG/JPEG 最长边限制至 1280px；PNG 透明度保留；GIF/WebP 原图保留动画及透明度，图片可辨认 |
| IMG-04 | 插入损坏图片或读取失败 | 显示错误并恢复编辑，不出现永久忙碌状态 |
| IMG-05 | 图片处理中离开页面或描述被外部更新 | 不把旧图片插入另一条待办，不覆盖更新后的内容 |
| IMG-06 | 复制富文本、代码与纯 Markdown 到编辑区 | 按源文编辑语义保留文本，不执行粘贴 HTML |
| UI-01 | 桌面分栏和窄屏分栏，打开帮助、输入长表格/长 URL | 分栏按宽度切换；工具栏换行；无不可达区域或页面溢出 |
| UI-02 | 深浅主题切换，检查代码高亮、链接、表格、选区 | 对比度足够，文字、边框与控件可辨认 |
| UI-03 | 输入约 100 KB Markdown 和多张图片，持续快速输入 | 无明显卡顿/丢字；延迟预览最终追上最新源文 |
| UI-04 | 打开预览后检查页面和控制台 | 页面非空、无框架错误遮罩、无相关运行时异常 |

## 6. 可复制语法样例

将下面代码框内全部内容作为描述输入。另行补充历史内嵌图片及损坏图片样本。

````markdown
# Markdown 验收
## 二级标题
### 三级标题
#### 四级标题
##### 五级标题
###### 六级标题

Setext 标题
===========

普通文字、**加粗**、*斜体*、***组合强调***、~~删除线~~、\*字面星号\*、&copy;。

硬换行第一行  
硬换行第二行。

- 一级列表
  - 二级列表
    - 三级列表

3. 从三开始
4. 下一项

- [ ] 待完成
- [x] 已完成

> 外层引用
>
> > 内层引用

---

行内代码 `a < b`，含反引号的代码 ``a ` b``。

```javascript
const message = "hello";
console.log(message);
```

```unknown-language
raw <text> & symbols
```

    indented code

| 左对齐 | 居中 | 右对齐 |
| :--- | :---: | ---: |
| **文字** | a\|b | 123 |

[普通链接](https://example.com "说明")、[引用链接][ref]、https://example.com。

[ref]: https://example.com/reference

带脚注的正文[^note]。

[^note]: 脚注内容。

行内公式 $E = mc^2$。

$$
\frac{a}{b} = \sqrt{x^2 + y^2}
$$

<details>
<summary>展开补充说明</summary>

补充 **Markdown** 内容。

</details>

<kbd>Ctrl</kbd> + <kbd>B</kbd>，H<sub>2</sub>O，x<sup>2</sup>。
````

## 7. 执行记录与通过条件

执行者逐项记录，不得根据代码实现将用例提前标为通过：

| 用例编号 | 环境/版本 | 实际结果 | 通过/失败/阻塞 | 证据或缺陷链接 |
| --- | --- | --- | --- | --- |
| 待填写 | 待填写 | 未执行 | 待执行 | — |

通过条件：P0 全部通过；目标语法与图片用例通过；浏览器及 Tauri 的保存重开、快捷键、撤销和脚注导航均有实际证据；构建成功；相关控制台错误已解决；剩余限制有明确记录。

当前交付不代表验收通过。尤其需要后续确认：KaTeX CSS 与渲染依赖版本兼容、WebView 原生撤销行为、外部数据更新后的表单同步、长文性能、窄窗口布局、桌面 SQLite 持久化，以及 Bun 锁文件同步。


## 8. 静态审查修复后的追加回归（未执行）

本轮合并公式 className 白名单，固定 KaTeX 为 0.16.47；预览切换保持 textarea 挂载；图片改为串行处理，GIF/WebP 保留原文件；未保存保护仅确认当前页离开行为。

| 编号 | 操作 | 预期结果 |
| --- | --- | --- |
| FIX-01 | 同一描述包含行内公式、块公式、带语言代码 | 公式正确识别；块公式居中独立展示；代码高亮不受影响 |
| FIX-02 | 输入数次文字并使用格式工具，切到预览再回编辑，撤销/重做 | 编辑 DOM 未重建，光标、滚动与原生撤销历史保留；Chromium 与 Tauri 分别执行 |
| FIX-03 | 插入动画 GIF、动画 WebP、透明 WebP | 原图数据保留；动画与透明度不被 JPEG 转换破坏 |
| FIX-04 | 选择 6 张图片、单张超过 10 MB 或合计超过 20 MB | 在读取文件前拒绝并提示；原描述不变 |
| FIX-05 | 选择 SVG 等不支持格式；快速重复触发插入 | 明确提示允许格式；同一时间只处理一批，串行处理不改变顺序 |
| FIX-06 | 未保存时点击脚注、脚注回链、新窗口外链、修饰键点击 | 不误弹离开确认，脚注不更改 HashRouter 路由 |
| FIX-07 | 未保存时点击当前窗口的其他页面链接；取消确认 | 仍显示离开确认；取消后不跳转，源文保留 |
| FIX-08 | 使用 npm 安装后检查公式渲染和 CSS 字体 | KaTeX 运行时与样式均使用 0.16.47，无版本错配 |

限制：GIF/WebP 为保留动画而不缩放；单张尺寸上限仅约束 PNG/JPEG。文件字节限制不能完全约束解码内存，异常超大像素图片仍需后续专项覆盖。`execCommand` 不可用时的 fallback 撤销行为仍需桌面实际验收。


## 9. 本轮实际验证结果（2026-09-08）

环境：macOS，Playwright 驱动独立无头 Google Chrome；`http://127.0.0.1:1420`；1440×900 与 390×844。Browser 插件不可用，使用 Playwright。仅使用浏览器示例数据和 tab-local sessionStorage，未操作桌面数据库。

构建：`npm run build` 通过（TypeScript + Vite）；窄屏修复后再次构建通过。主 JS 包约 1,572 KB、gzip 484 KB，存在超过 1,500 KB 的警告。

本轮浏览器检查：30 项通过，0 项失败。覆盖：

- 页面身份、非空页面、无框架错误遮罩、控制台无错误。
- 预览切换保留 textarea 节点，切换后的原生撤销/重做可用；工具栏选区替换和撤销通过。
- 行内与块级公式、代码高亮、GFM 表格、任务复选状态、安全 HTML、details 展开。
- 脚注与回链不跳路由、不弹离开确认；新窗口外链不弹确认；当前窗口导航仍确认，取消保留源文。
- GIF 原始字节保留并成功加载；透明 WebP 原始字节保留。
- 超过 5 张、单张超过 10 MB、批次超过 20 MB 均拒绝且保留原描述；SVG 拒绝；损坏 PNG 报错后恢复编辑。
- Markdown 源文保存至浏览器 sessionStorage 后逐字符一致，重新打开待办仍一致。
- 窄屏页面无横向溢出，模式按钮可见。

首轮发现与处理：

1. 窄屏侧栏展开时模式按钮被裁切：模式按钮组允许换行，工具栏不再被纵向压缩，编辑区域允许纵向滚动；复测通过。
2. 撤销测试原先错误假定一次撤销会删除整段逐字输入；Chrome 实际按字符分组。修正为检查撤销产生历史回退且重做恢复原文，未修改产品撤销逻辑。

未完成或已知限制：

- 未启动 Tauri，未验证 macOS WebView 原生撤销/外链打开和 SQLite 保存重启。
- GIF 使用小型有效样本，WebP 使用透明样本；虽然原始字节已确认保留，未逐帧验证真实动画播放。
- 尚未覆盖全部测试矩阵：超大像素图、PNG/JPEG 缩放、粘贴通道、中文组合输入、长文性能及外部状态更新等仍待执行。
- 深色截图中 Markdown 正文/公式/高亮可读，但部分工具栏按钮及页面表单控件对比度不足；整个深色主题不能标记为视觉验收通过。
- 390px 窗口展开侧栏时内容区仍很窄；本轮修复确保控件不被裁切，整体窄屏导航体验仍可优化。
- 主包体积警告未处理；Bun 锁文件未同步。

临时证据位于 `/private/tmp/todo-md-results.json`、`/private/tmp/todo-md-build.log`、`/private/tmp/todo-md-desktop.png`、`/private/tmp/todo-md-mobile.png`、`/private/tmp/todo-md-dark.png`，临时目录清理后不保证保留。


## 10. 新增编辑交互（待验证）

本轮按要求仅修改代码，未运行构建或测试；第 9 节的 30 项通过记录只针对上一轮版本，不能作为本轮验收结果。

- 图片处理状态同步到详情页，保存按钮禁用；提交入口再次检查忙碌状态。保存进行中禁止描述编辑和图片插入。
- 加粗、斜体、删除线支持再次应用取消；标题/引用/列表支持移除前缀，列表类型切换替换旧标记。
- 行内代码根据选区内反引号长度选择围栏，处理首尾反引号和空格。
- 列表回车续写，有序编号递增、任务项续写为未完成；空项回车退出。围栏代码内保留原生回车。
- 列表、围栏代码和多行选区支持 Tab/Shift+Tab 缩进；普通段落保留 Tab 焦点导航。
- 链接输入窗口支持显示文字、地址、常规内联链接编辑、取消与错误提示；关闭弹窗后再插入并恢复焦点。

后续重点用例：

| 用例 | 预期 |
| --- | --- |
| 图片处理时点击保存或触发表单提交 | 不保存未完成描述，处理完成后恢复保存 |
| 保存过程中粘贴图片 | 不启动新插入，不覆盖正在保存的 note |
| 格式连续点击两次，包括全标记选中/仅内文选中 | 恢复原文；嵌套格式和混合格式另行验证 |
| 标题级别切换、无序/有序/任务列表切换 | 保留缩进和正文，旧标记不叠加 |
| 行内代码包含一至多枚反引号、首尾空格 | 源文合法，预览内容符合 CommonMark |
| 列表行尾和行中回车、空任务项回车 | 正确续写或退出；不误提交表单 |
| 代码围栏内回车、普通文字 Tab、列表缩进 | 分别保持换行、焦点导航和层级调整 |
| 链接弹窗确认、取消、Escape、Enter、中文输入法确认 | 无误提交任务，光标恢复；确认插入可撤销 |
| 链接地址含空格/括号，文字含方括号；危险协议和不完整网址 | 正确编码或明确拒绝，源文不丢失 |
| 弹窗打开后描述外部更新 | 不按旧位置覆盖新内容 |

边界：链接编辑目前识别常规内联链接，不承诺自动编辑引用式链接、带 title 的链接或任意嵌套结构；这些语法仍可直接修改源文。列表快捷操作属于文本辅助，复杂引用内列表等组合仍需专项覆盖。
