# UI 改造交接（当前进度版）

日期：2026-09-07。用户要求暂停后续调试并打包。当前不是全部验收完成的最终发布版。
原始输入：/Users/unstable/Downloads/scripts.zip。修改在独立副本完成，未覆盖原压缩包或原项目。

## 已实现
- 浅灰背景、白色卡片、靛蓝主色，统一边框、焦点、导航和按钮。
- 项目看板：独立任务卡片、泳道把手、数量、新增入口、搜索与分支筛选。
- 泳道左右拖拽、同泳道任务排序、跨泳道指定位置移动、空泳道落点、插入线和拖拽浮层。
- 跨泳道任务移动和两边顺序在单个 store action 中更新；泳道顺序保存。
- TodoRow 将展示与拖拽外层分离，支持 card/list；保留原有任务/Git操作并增加移动到泳道菜单。
- 今日焦点：限制宽度、待处理及可折叠完成分组，保留原筛选集合。
- 项目资料：卡片、任务总数文案、更多菜单、搜索、归档查看/恢复。
- 待办编辑：文档区与属性面板；泳道/代码分组；路径完整显示；保存中锁；基础未保存提示；修复默认值初始化、自定义路径输入持续性。
- 项目弹窗：基础信息与高级配置分区；Token 显隐；分支规则上下移；固定页头页脚、正文滚动。
- 修复项目弹窗 overflow:hidden 导致自动定位控件时整个弹窗滚动的问题，使用 overflow:clip。
- 泳道管理：名称/状态完整性校验、排序按钮可访问名称；最终泳道配置与任务迁移一次更新。
- 浏览器使用隔离示例数据，sessionStorage 仅保留当前标签页的刷新状态，并剔除 Token；关闭标签后不保留。桌面端仍使用原 Tauri/SQLite 持久化。

## 已验证
- 最后一次 npm run build 成功（TypeScript + Vite）。存在约 1MB 主 JS chunk 体积提示，无构建错误。
- node scripts/test-board-order.mjs 通过：同列上/下移、跨列插入、空列、状态同步、项目隔离、不修改原数据、序列化和泳道排序。
- 浏览器首轮：泳道左右拖拽、同列下移、跨列中间插入、空泳道接收、刷新后保留顺序。
- 从泳道新增任务能预选正确泳道；直接打开任务页时等待 store 初始化后取默认分支。
- 必填校验、自定义目录输入、日期范围选择、保存任务、重新打开正文和日期保持。
- 新建项目成功；分支规则下移生效；长弹窗展开后页头和底部按钮保持可见。
- 看板搜索、今日焦点已完成分组已操作验证。
- 初轮看板控制台未见 error/warn。后续整体改动尚未统一复核控制台。

## 尚未完成（下次优先做）
1. 最终全量浏览器回归：后续保存泳道逻辑、菜单移动、归档恢复等新增改动还未逐项完整复测。
2. 拖拽边界：连续多次反向移动、边缘自动滚动、取消拖拽、键盘操作、筛选时行为和按钮点击不误触的系统验证。当前筛选状态禁用任务拖拽，清除筛选后恢复。
3. 桌面真实环境：未运行 Tauri 桌面版，未验证真实 SQLite 关闭重开、外部同步竞争，以及 Git 创建/切换分支、同步/补录提交等操作。浏览器验证不能替代这些。
4. 视觉收尾：五页小窗口/缩放、长文本、大量任务与规则、全部深色皮肤需要完整检查。深色切换已尝试，最终对比度和截图还未完成确认。
5. 更完整的未保存保护：目前有页面退出、链接导航与返回按钮提示；浏览器历史返回等路径尚未完整覆盖。
6. 代码整理：部分新组件为较紧凑写法，格式化未完成；主 bundle 体积优化未做。
7. 尚未形成完整的最终 design-qa 报告，也未打包原生安装程序。

## 运行

在本文件所在目录执行：

```sh
npm ci
npm run dev
```

默认端口 1420。浏览器看板路径：/#/project/demo-project。
桌面开发沿用原项目方式：npm run tauri dev，需要 Rust/Tauri 与系统工具链。

校验：

```sh
npm run build
node scripts/test-board-order.mjs
```

包中包含当前源码、原 Rust 后端、锁文件、文档、测试脚本和最后构建的 dist；不含 node_modules、Git 历史或用户业务数据库。
Vite 的 usePolling 当前为 true，是本次环境文件监测的兼容设置；如本机热更新正常，可恢复默认文件监测。

## 后续工作要求
继续沿用用户确认的效果图风格，保留三种拖拽与原有 Git 业务行为。不要把浏览器示例数据当成用户真实数据，也不要把这份进度记录当成已经全部验收通过。
