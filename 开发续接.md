# 开发续接 · 2026-09-10（第二轮）

## 用户要求

开发应用功能，明确排除验收清单和里程碑。所有工作只改代码；本轮只运行了前端 `tsc`（`node_modules/.bin/tsc --noEmit`，已通过），未运行 `cargo check` / `cargo test` / `cargo clippy` / 构建 / 应用。
仓库：/Users/unstable/Documents/ChatGPT/TO DO/todo-kanban。现有未提交代码保留，未执行 git 提交或推送。

## 本轮修复（接着上一轮「暂停前最后一批修改」逐行复核）

### 编译阻塞与版本一致性

1. `config/src/lib.rs`：`MIGRATION_STEPS` 声明为 `[(&str,&str); 8]` 却有 9 项（含 v9→v10）——**这是编译错误**，已改为 `; 9`；`DATA_VERSION_RANGE` 由 `v1 ~ v9` 改为 `v1 ~ v10`；CHANGELOG 补 v9（资料库）与 v10（工作流）条目。
2. `core/src/db/schema.rs`：`USER_VERSION` 9 → 10，与 `config::CURRENT_DATA_VERSION` 对齐（否则迁移后 `open_existing` 会因版本号不匹配拒绝跨进程访问）；注释注明两者必须一致，并合并 v9/v10 的建表说明。
3. 文档注释同步真实数量：`src/lib.rs`（39 个 handler）、`commands.rs`（30 条命令）、`bridge.rs`（11 tools + 4 resources）。顺带修掉上一轮遗留的前端编译错误：`SidebarLayout` 用了 lucide-react 已移除的 `KanbanSquare`，改为 `SquareKanban`。

### db 保存链路与备份恢复

4. `save_state_extended` / `save_state_inner` / `svc/db_cmds.rs::save_extended` 新增 `workflow_revision: Option<i64>`：恢复备份时在同一事务内比对当前配置版本，不一致返回 `STATE_CONFLICT: 工作流配置已变化`，不再静默覆盖并发的工作流修改。`restore_snapshot` 因此要求显式传入 revision。
5. `backups.rs` 重写恢复链路，顺序为：清单校验 → 备份附件自检 → 业务快照 expected 校验 → 恢复前安全备份 → 附件文件同步 → 单事务写库 → **事务成功后**才回收多余附件文件。
   - 新增 `verify()`：备份目录必须同时有 manifest.json 与 state.db，附件文件数/字节数必须与清单一致，缺失即拒绝恢复。
   - 新增 `collect_files()`：递归收集（跳过 trash），遇符号链接报错；`verify` 与回收共用。
   - `copy_tree()`：同名文件改为**流式内容比较**（`same_file` + `read_block`，不再整文件读入内存）；同名异内容仍然停止恢复且不覆盖任何当前文件。
   - `AttachmentSnapshot::read()` 增加「附件相对路径首段必须是快照中仍存在的任务」校验，避免恢复出指向不存在任务的附件行。
   - 新增 `reconcile_attachments()`：写库成功后把「备份中不存在」的当前附件文件移入 `attachments/trash/`（可人工找回），避免恢复后残留孤儿文件。
6. `same_state` 语义未变（按 id 排序比较，数组顺序不敏感），新增单测覆盖。

### 提醒与保留策略

7. `workflow.rs` 提醒领取改为**租约**语义：`claim_reminders` 领取超过 `CLAIM_LEASE_MS`（10 分钟）仍未投递的提醒会重新入列，进程在领取后退出不再永久漏提醒；`retry_reminder` 延时 60 秒重排。
8. 新增 `sweep_reminders()`：启动后清理过期提醒（已投递且早于 7 天的删除，超期未投递的标记为已处理）；`native_workflow::start` 首次循环执行。
9. 新增 `workflow::trim()`：`change_history` 只保留最近 20 000 条、已处理的提案保留 200 条，**待批准提案永不静默丢弃**；在保存事务内调用。
10. `claim_reminders` 关闭开关时不再提前 return（避免留下未提交的 Immediate 事务），改为显式收尾。

### 桌面壳

11. `native_workflow.rs` 重写安装路径：tray 与全局快捷键失败只写入降级状态（`trayError` / `shortcutError`，经 `desktop_status` 暴露到工作流页），**不再让应用启动失败**；`quick_add` 修正「未就绪就清挂起标记」的竞态（未就绪只挂起、不发事件）。
12. `src/lib.rs`：`native_workflow::install(_app)?` → 不传播错误。

### 前端

13. `src/lib/workflow.ts`：`publish` 增加**单调 revision**，低于当前 revision 的迟到响应直接丢弃（此前读取响应可能覆盖刚保存的配置）；`loadWorkflow` 并发合并且晚到不覆盖；新增 `refreshWorkflow` 失败重试；`saveWorkflow` 返回保存后的配置；`applyChange` 统一带上 `workflowRevision`；`blockers` 去掉旧缓存实现；新增 `DesktopStatus` 类型。
14. 资料直接阅读入口：新增 `components/workflow/ResourceDetailDialog.tsx`（只读全文 + 外链 + 标签，可选「关联到当前任务」），并在 `TaskRelations` 里把资料标题变成可点阅读入口（此前只能靠资料库的项目筛选间接查看）。
15. `TaskRelations.tsx`：拆分阅读弹窗状态，关联开关改为统一的 `toggleResource`；依赖/子任务行为不变。
16. `WorkflowLifecycle.tsx`：快速添加草稿写 `sessionStorage`（`todo-kanban.quick-add.draft.v1`，24 小时有效，`beforeunload` 兜底，保存/放弃时清除），快捷键与菜单栏事件的 `listen` Promise 都补了 `.catch`；菜单栏「今日清单」增加**跨天刷新**（到本地零点重新计算并重建清单）；快速添加新建任务改为传空 tag，让后端按 seq 生成 `todo-<seq>`。
17. `WorkflowPage.tsx`：通知开关先读取 `checked` 再 await 权限请求；所有保存改用 `live()`（实时配置）而不是渲染时的闭包，消除旧 revision 导致的保存被拒/覆盖；`ProposalDiff` 用 `asRecord` 收敛类型断言；备份区文案补充配置版本校验说明。
18. `TaskReminder.tsx`：改用 `getWorkflow()` 现读现写。
19. `normalize.ts`：`normalizeTodo(undefined)` 不再把 `id` 前 8 位当 tag（避免制造错误提交标记）；显式空串继续交给后端生成；`todoTag` 保留导出供旧数据兜底。

## 本轮新增测试（未运行）

- `db/mod.rs`：`same_state_ignores_collection_order`、`restore_snapshot_guards_workflow_revision`（过期 revision 拒绝且业务数据不变、匹配时推进到 revision+1）、`history_trim_keeps_recent_and_pending_proposals`。
- `svc/workflow.rs`：`claim_lease_allows_reclaim_after_crash`、`claim_skips_done_and_archived`、`claim_ignores_disabled_switch`、`sweep_drops_stale_and_keeps_recent`、`prune_clears_cross_project_parent_only`。
- `svc/backups.rs`：`attachment_snapshot_rejects_missing_and_orphan_files`、`copy_tree_stops_on_conflicting_content`、`copy_tree_skips_trash_directory`（配套新增 `attachments::import_b64_at` 供测试指定库路径）。

## 下次优先继续

### 必须先跑一遍门禁（本轮未运行）

1. `cd src-tauri && cargo check`（重点：rusqlite `Transaction` 与 `Connection` 的借用、`MIGRATION_STEPS` 新长度、备份模块新签名），再 `cargo test -p todo-kanban-core`、`cargo test -p mcp-server`、`cargo clippy -- -D warnings`、`cargo fmt`。
2. `npm run build`（前端 tsc 已通过，仅剩 vite 构建与 vditor 资产同步）。
3. 首次真实启动会执行数据库 v9/v10 升级（新库直接 v10；旧库逐级迁移），确认 `USER_VERSION` 与 `config::CURRENT_DATA_VERSION` 一致后可正常打开；MCP server 同样依赖该版本号。

### 功能收尾（仍未做）

- 备份清单没有记录「备份时的工作流 revision」，恢复后统一取当前 revision+1；如果希望备份本身能还原工作流配置版本语义，需要在 manifest 增加字段（会破坏旧清单兼容，需给默认值）。
- 恢复过程中的附件文件同步仍是「先落盘、再写库」：写库若因 expected 冲突失败，会留下备份带来的新文件（未删，注释已说明）。可考虑失败时回滚这些新文件（需要「文件是否本次新建」清单已经收集好，`copy_tree` 的 `created` 出参已具备）。
- 提醒只覆盖「应用运行时」与「下次启动补发」；领取租约解决了进程崩溃漏提醒，但没有做「发送成功确认」的二次回执。
- 工作流保存目前是整份覆盖 + revision 乐观锁；若两个窗口同时编辑不同字段，后者会被拒绝而不是合并（与业务快照的 expected 语义一致，暂不打算改成字段级合并）。
- `desktop_update_tasks` 只显示 12 条；菜单栏清单排序规则（进行中优先 → 逾期 → 今日创建）如需调整在 `WorkflowLifecycle` 第二个 effect 内。
- 资源详情弹窗只读；若要在弹窗内直接编辑资料，需要复用 `ResourceFormDialog` 并注意 store 写链。
- MCP 提案仍是整份批准/拒绝，未做逐字段选择（用户未要求）。
- `docs/README.md`、`docs/软件设计文档.md` 中「9 tools / 3 resources」「11 个 Tauri 命令」等数字已过期，需要与 `bridge.rs`（11 tools/4 resources）、`commands.rs`（30 命令）对齐。

## 核心文件（本轮改动）

后端：`config/src/lib.rs`、`core/src/db/schema.rs`、`core/src/db/mod.rs`、`core/src/svc/db_cmds.rs`、`core/src/svc/backups.rs`、`core/src/svc/workflow.rs`、`core/src/svc/attachments.rs`、`src/commands.rs`、`src/lib.rs`、`src/native_workflow.rs`、`mcp-server/src/bridge.rs`。
前端：`src/lib/workflow.ts`、`src/lib/normalize.ts`、`src/components/workflow/ResourceDetailDialog.tsx`（新增）、`TaskRelations.tsx`、`TaskReminder.tsx`、`WorkflowLifecycle.tsx`、`src/pages/WorkflowPage.tsx`、`src/components/layout/SidebarLayout.tsx`。

## 状态边界

这些功能仍是代码实现状态：**只做过前端 tsc 类型检查**。Rust 侧未编译、未跑测试、未跑 clippy，数据库升级未在真实数据上执行，桌面托盘/通知/快捷键未在真机验证。
